# Figma Design Integration & References

Figma is the primary visual source of truth for the Taskly project.

## File Reference

- **File Name**: `Taskly — Tasks Management`
- **File Key**: `oL1WORO4G2iuQfSGPd4qvS`
- **Base URL**: `https://www.figma.com/design/oL1WORO4G2iuQfSGPd4qvS/Taskly--Tasks-Management---Copy-`

## Key Screen Node IDs

- **Sign Up (Desktop)**: `1:1219` (`node-id=1-1219`)
- **Design Variables / Setup**: `76:1757` (`node-id=76-1757`)

## Inspection Workflow via Figma Desktop MCP

When implementing UI components or screens, inspect the relevant node using Figma MCP tools:

1. `get_design_context`: Retrieve layout hierarchy, dimensions, typography, and color tokens for the specific node ID.
2. `get_screenshot`: Visually inspect the node rendering for context.
3. `get_variable_defs`: Retrieve any design variable bindings if defined.

> [!NOTE]
> Do not duplicate full component trees or static layout measurements into local documentation. Use the Figma MCP tools directly for real-time inspection.
